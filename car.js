//the class defined here is private so we add export to make it public
//to make car default use default
//export default class Car extends Bike
export function manny(){}
export class Car{
    constructor(name){
       this.name=name;
    }
           horsepower() {
               console.log(this);
           }
   }

const profile={
    id:'',
    name:'',
    gender:'',
}
//to fill it up use.
const gender=profile.gender
const id=profile.id
const name=profile.name
//insted we can use reconstructing

const {gender,id,name}=profile
//we can setting name property to nam
const {name:nam}=profile